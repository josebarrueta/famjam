apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-site
  labels:
    {{- include "rallyroo.labels" . | nindent 4 }}
data:
  {{- range $path, $_ := .Files.Glob "files/site/*" }}
  {{ base $path }}: |-
    {{- $.Files.Get $path | nindent 4 }}
  {{- end }}
